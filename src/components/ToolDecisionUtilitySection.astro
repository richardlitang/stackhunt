---
interface SetupCard {
  title: string;
  body: string;
  verificationLabel?: string;
  costTrigger?: {
    text: string;
    status: 'Source-backed' | 'Needs confirmation';
    evidenceHref?: string;
  };
}

interface Props {
  checklistTitle: string;
  checklistItems: string[];
  commonSetupsTitle: string;
  commonSetups: SetupCard[];
}

const { checklistTitle, checklistItems, commonSetupsTitle, commonSetups } = Astro.props as Props;
---

<section
  id="decision-utility"
  class="mb-6 rounded-xl border border-zinc-700 bg-zinc-900 p-5 shadow-sm"
  data-focus-target
  data-focus-groups="all verdict setup pricing"
>
  <details class="group rounded-lg border border-zinc-800 bg-zinc-950/40 p-3">
    <summary class="flex cursor-pointer list-none items-start justify-between gap-3">
      <div>
        <h3 class="text-sm font-semibold text-zinc-100">{checklistTitle}</h3>
        <p class="mt-1 text-xs text-zinc-500">
          Verify these points before migration effort is committed.
        </p>
      </div>
      <span class="inline-flex items-center gap-2 text-xs text-zinc-400">
        <span class="group-open:hidden">Expand</span>
        <span class="hidden group-open:inline">Collapse</span>
      </span>
    </summary>
    <ul class="mt-3 space-y-2 text-sm text-zinc-300">
      {checklistItems.map((item) => <li>- {item}</li>)}
    </ul>

    {
      commonSetups.length > 0 && (
        <div class="mt-5 border-t border-zinc-800 pt-4">
          <h4 class="text-sm font-semibold text-zinc-100">{commonSetupsTitle}</h4>
          <div class="mt-3 grid gap-3 md:grid-cols-3">
            {commonSetups.map((setup) => (
              <article class="rounded-lg border border-zinc-800 bg-zinc-950/30 p-3">
                <h5 class="text-sm font-semibold text-zinc-100">{setup.title}</h5>
                <p class="mt-2 text-sm text-zinc-300">{setup.body}</p>
                {setup.costTrigger && (
                  <p class="mt-2 text-xs text-zinc-400">
                    Cost trigger:{' '}
                    {setup.costTrigger.evidenceHref ? (
                      <a
                        href={setup.costTrigger.evidenceHref}
                        class="text-hunt-500 hover:text-hunt-400"
                      >
                        {setup.costTrigger.text}
                      </a>
                    ) : (
                      <span>{setup.costTrigger.text}</span>
                    )}
                  </p>
                )}
                {setup.verificationLabel && (
                  <p class="mt-2 text-xs text-amber-300">{setup.verificationLabel}</p>
                )}
              </article>
            ))}
          </div>
        </div>
      )
    }
  </details>
</section>
