---
import { resolveFreshnessLine } from '@/lib/tool-page/freshness-line';

interface Props {
  status: 'Source-backed' | 'Needs confirmation' | 'Needs recheck';
  confidence: 'High' | 'Medium' | 'Low';
  lastChecked?: string | null;
}

const { status, lastChecked = null } = Astro.props;
const freshnessLine = resolveFreshnessLine({ lastCheckedISO: lastChecked, status });
---

<div class="tool-review-panel-muted rounded-2xl px-4 py-3 text-xs text-zinc-300">
  {freshnessLine || 'Verification details are available in the methodology section.'}
</div>
